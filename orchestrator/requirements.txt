# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
python-multipart>=0.0.9  # FastAPI File/UploadFile support
httpx>=0.27.0
python-docx>=1.1.0       # extract URLs from .docx uploads (optional)

# LLM providers
anthropic>=0.50.0
openai>=1.55.0
google-genai>=2.0.0

# Observability (opt-in; agents work without)
langfuse>=2.35.0

# Outcome DB (M3+; harmless to install in M2)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# M4.1 / ADR-019 — Cazador Fase 3: aprendizaje de preferencias
# OPCIONALES: el módulo preferences.py funciona sin estos (fallback determinista
# hash-based). Instálalos para activar embeddings reales en producción:
# sentence-transformers>=3.0        # 80MB modelo multilingual ES-friendly
# hdbscan>=0.8                       # clustering jerárquico
# scikit-learn>=1.4                  # TF-IDF + cosine similarity

# dev / test
pytest>=8.2.0
pytest-asyncio>=0.23.0
mypy>=1.10.0
